apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  namespace: chatops
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: chat-service
  template:
    metadata:
      labels:
        app: chat-service
    spec:
      containers:
      - name: chat-service
        image: {{ .Values.chatService.image }}
        imagePullPolicy: {{ .Values.chatService.imagePullPolicy }}
        ports:
        - containerPort: {{ .Values.chatService.port }}
        env:
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: chatops-secrets
              key: jwt-secret
        resources:
          requests:
            memory: {{ .Values.resources.requests.memory }}
            cpu: {{ .Values.resources.requests.cpu }}
          limits:
            memory: {{ .Values.resources.limits.memory }}
            cpu: {{ .Values.resources.limits.cpu }}
        readinessProbe:
          httpGet:
            path: /health
            port: {{ .Values.chatService.port }}
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: {{ .Values.chatService.port }}
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: chatops
spec:
  selector:
    app: chat-service
  ports:
  - port: {{ .Values.chatService.port }}
    targetPort: {{ .Values.chatService.port }}
  type: ClusterIP
