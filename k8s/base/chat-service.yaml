apiVersion: apps/v1
kind: Deployment
metadata:
  name: chat-service
  namespace: chatops
spec:
  replicas: 2
  selector:
    matchLabels:
      app: chat-service
  template:
    metadata:
      labels:
        app: chat-service
    spec:
      containers:
      - name: chat-service
        image: chatops/chat-service:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 8002
        env:
        - name: JWT_SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: chatops-secrets
              key: jwt-secret
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "500m"
        readinessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 10
          periodSeconds: 5
        livenessProbe:
          httpGet:
            path: /health
            port: 8002
          initialDelaySeconds: 30
          periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: chat-service
  namespace: chatops
spec:
  selector:
    app: chat-service
  ports:
  - port: 8002
    targetPort: 8002
  type: ClusterIP
